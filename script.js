const size = 16

function createGrid (numOfSquares) {
    for (i=0; i<size; i++) {
        document.createElement(div)
    }
}
const square = document.createElement(div)


// This number will be a user input of some kind


createGrid(size)